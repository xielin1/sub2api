# AGENTS

本仓库是 Wei-Shaw/sub2api 的 fork；`mine` 保留现有补丁，后续直接合并 `upstream/main`，不再从 `klno` 同步。

- 写码前读 `docs/conventions/codex-outbound-identity.md`；碰账号 extra 读写再加 `docs/conventions/account-extra-freshness.md`；续接工作先看 `docs/dev-journal.md` 最新条目和 `docs/tasks/`。
- 非平凡改动后回写 journal；产生新约定就改写 conventions（不是追加）。
- 补丁必须小、便于与官方主线合并；协议事实以 openai/codex 源码为准。
